import Models.CharacterCosmetics.CosmeticDetails;
import Gui.ActionProcessors.SaveFileProcessor;
import Gui.Gui;
import Models.RulesLogic.Abilities;
import interfaces.iModelFactory;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.powermock.core.classloader.annotations.PowerMockIgnore;
import org.powermock.core.classloader.annotations.PrepareForTest;
import org.powermock.modules.junit4.PowerMockRunner;

import static org.mockito.Mockito.*;

@RunWith(PowerMockRunner.class)
@PrepareForTest(characterSheet.class)
@PowerMockIgnore("javax.management")
public class TestCharacterSheet {
    String name;
    String player;
    String god;
    String[] params;
    Gui mockGui;
    iModelFactory mockFactory;
    characterSheet manny;

    @Before
    public void setUp() {
        name = "Manetherin";
        params = new String[] {name, player, god};
        mockGui = mock(Gui.class);
        mockFactory = mock(iModelFactory.class);
        manny = new characterSheet(mockGui, mockFactory);
    }

    @Test
	public void testGenerateGui() {
		manny.generateGui();
        verify(mockGui, times(1)).run(mockFactory);
	}



































































































































































































































































































































































































































































































































































































































































































}